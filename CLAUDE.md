# Project rules

Follow the user's current instructions and the routing description of the installed skill.
Keep workflow sources, generated copies and their tests consistent. Preserve unrelated changes and
user data. Managed 0.4 runs belong to the pinned 0.4.3 runtime; see MIGRATION.md.
