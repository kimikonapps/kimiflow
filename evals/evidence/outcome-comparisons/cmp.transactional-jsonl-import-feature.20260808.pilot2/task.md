Implement the complete transactional JSONL import feature in `FEATURE.md`.

Preserve the existing public API and CLI behavior, add regression tests, and run the full test suite. Use
only the Python standard library. Do not edit `README.md`, `FEATURE.md`, `TASK.md`, or `.gitignore`. Create
one local git commit containing the completed implementation after all tests pass.
