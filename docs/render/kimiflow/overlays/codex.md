---
name: kimiflow
description: "Minimal delivery support for substantial authorized feature work with material integration, data, security, architecture or discovery risk. Explicit Kimiflow selects it; direct/direkt bypasses it. Ordinary fixes, reviews, refactors, docs, small changes and discussion stay direct."
---

# Kimiflow for Codex

Read `SKILL.md` at the installed plugin root once. The root is two levels above this file.
Use the user's chosen model, Codex's task history, plan/tools and workspace isolation. Do not create
phase state, a reviewer ensemble or memory ledgers. Resolve helper paths against the installed plugin,
never a project's similarly named scripts.

For legacy Flow-schema state or managed `workflow_context`, read installed `MIGRATION.md`; the old
controller is not part of this version. Ordinary new tasks follow the short shared working agreement.
