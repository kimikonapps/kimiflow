# R2.1 Rule Inventory — No Prose Movement

**Date:** 2026-07-02 · **Branch:** `rebuild/r2-prose` · **Status:** generated inventory for Commit R2.1. This artifact records current rule locations only; it does not approve or perform any movement from `SKILL.md` or `reference.md`.

## Completeness Contract

- Source corpus combines `docs/superpowers/plans/2026-07-02-token-restructuring-invariants.md`, the strengthened `docs/superpowers/plans/2026-07-02-invariant-check.sh`, and the literal phrase contracts in `hooks/smoke-install.sh` / `hooks/smoke-install-codex.sh`.
- `docs/superpowers/plans/2026-07-02-invariant-corpus.tsv` is the immutable protected-ID source for R2. Target maps must join to it and grep its `strong_needle`; target maps may not override needles.
- All non-empty `SKILL.md` lines are listed in the line coverage table below. `heading/context` and `example/prose-only` are coverage labels only, not permission to delete or move text.
- R2.1 moves no prose. Proposed homes are candidate homes for the later approval packet only.

## Summary

- Protected corpus rows: 148
- Non-empty `SKILL.md` lines covered: 165 / 165
- `SKILL.md` lines containing exact corpus needles: 93
- `needs-review` rows: 0

## Protected Corpus Classification

| ID | Class | Current Target | Proposed Home | Enforcement Boundary | Strong Needle | Rationale |
|---|---|---|---|---|---|---|
| INV-001 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Do NOT auto-trigger on ordinary feature/bug/refactor requests` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-002 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never writes code directly and never auto-picks a risky action` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-003 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `otherwise ask one plain-language question` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-004 | `CORE-ALWAYS+GATE-CHECKABLE` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `STOP at the pre-build approval gate` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-005 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `then STOP. No plan and no code` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-006 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `STOP with a resumable backlog run` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-007 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `do not silently invent a plan` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-008 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Never use when the user asked for` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-009 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `ONE code-review lens` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-010 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `No code edits` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-011 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `no edits until the user chooses a slice` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-012 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `phases 0–4, then STOP` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-013 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `blind implementation is forbidden` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-014 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `shows them for approval` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-015 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `It does not edit code` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-016 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never auto-committed` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-017 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `exploit paths` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-018 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never persisted` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-019 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `HARD RULE` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-020 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `NEVER paste a full artifact into chat` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-021 | `GATE-CHECKABLE` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Gate verdict = ONE line` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-022 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Narration ≠ persistence` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-023 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Density NEVER costs rigor` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-024 | `CORE-ALWAYS+GATE-CHECKABLE` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never for gate criteria, scores, or thresholds` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-025 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `No speculative abstractions, no features beyond the request` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-026 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Severity never higher than provable` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-027 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `done/green/root cause found` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-028 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Beyond ~10 → stop and ask the user first` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-029 | `GATE-CHECKABLE` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `blocks the review-gate call` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-030 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/active-run.sh start --run .kimiflow/<slug>` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-031 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/background-run.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-032 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `cannot be applied blindly` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-033 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/agentic-readiness.sh status\|gate` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-034 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Never loop forever` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-035 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `verbatim only for a snippet under ~15 lines` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-036 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/launcher-status.sh --pretty` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-037 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `do not auto-pick` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-038 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/working-tree-gate.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-039 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `STOP before changing files` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-040 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `ask one simple question` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-041 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `requires a target path` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-042 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `refresh-baseline --write` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-043 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `git rev-parse --is-inside-work-tree` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-044 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `In doubt, the smaller tier` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-045 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `resolve-verbosity.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-046 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `you MUST ask once` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-047 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `project-map-status.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-048 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `PMS coverage --affected` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-049 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `one plain-language question only if the request lacks` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-050 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never auto-pick` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-051 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Does this match?` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-052 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Did I understand the problem correctly?` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-053 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Is this the right cleanup scope?` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-054 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/clarify-gate.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-055 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `<!-- kimiflow:clarify-evidence mode=questions count=2 confirmed=yes source=current-run -->` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-056 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/memory-router.sh status` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-057 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `MR recall --query-file` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-058 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `record the graceful skip and continue` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-059 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `current-state-gate.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-060 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `CSG verify --assessment` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-061 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `suggest-affected-sections.sh --intent` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-062 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `a plan-blocking unknown → resolve first` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-063 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `before changing production code` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-064 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `find AND prove the cause` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-065 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `root cause not proven → do NOT fix` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-066 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `should this exist at all` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-067 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `repo-wide pre-delete grep` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-068 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `git-history-freshness` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-069 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Caller-grep is a MINIMUM` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-070 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `only if a vault MCP is connected` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-071 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never structural merges` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-072 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `do not send it to reviewers` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-073 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/plan-blocker-gate.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-074 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `required before spawning reviewers` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-075 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `A cut survives only if no reviewer finds one` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-076 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `tests green before+after` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-077 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `No self-reported count` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-078 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/resolve-review-gate.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-079 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `--round <N> --expect <lensCSV>` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-080 | `CORE-ALWAYS+GATE-CHECKABLE` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `stop + ask, gate CLOSED` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-081 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never auto-proceed` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-082 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Status: backlog` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-083 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `resolve-build-gate.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-084 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Approve to build, change something, or defer to backlog?` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-085 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `do NOT build` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-086 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `production code never rides along` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-087 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `no proof → don't delete` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-088 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `don't burn a blind second attempt` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-089 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/red-green-gate.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-090 | `GATE-CHECKABLE` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `--mode fix` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-091 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `hooks/lsp-diagnostics.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-092 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `re-run the decisive command` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-093 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `back to phase 5` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-094 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `--kind review --write` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-095 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `review light` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-096 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `bug-regression` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-097 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `add the third when the diff touches hooks` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-098 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `CANDIDATE <SEVERITY> <ref> :: <claim> :: verify=<smallest check>` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-099 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `active refutation attempt` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-100 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never raw candidates` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-101 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never silently skip the advisory channel` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-102 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `test-weakening-scan.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-103 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `secret-content-scan.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-104 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `--expect code-verified` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-105 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Wait for explicit OK` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-106 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `no co-author/AI trailer` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-107 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never staged or committed` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-108 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Never batch slices` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-109 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `MR review-run --run` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-110 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `--skip "<reason>"` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-111 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `a CLOSED result blocks completion` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-112 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `neither `.kimiflow/` nor repo files store the key` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-113 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never patches skills or writes external notes blindly` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-114 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `SKILL-DRAFTS` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-115 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `PMS refresh --changed` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-116 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `map-staleness-nudge.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-117 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `mark-done <id> --commit <sha> --write` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-118 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never auto-guess` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-119 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `improvements-staleness-nudge.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| INV-120 | `CORE-ALWAYS+GATE-CHECKABLE` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Only after the commit gate and learning review are open` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-121 | `CORE-ALWAYS+GATE-CHECKABLE` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never gates, cost, quality, or behavior` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-122 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `BEFORE fan-out` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-123 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `uncommitted` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| INV-124 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `never wired into CI` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-001 | `SMOKE-CONTRACT` | `SKILL.md` | SKILL.md | smoke scripts | `Launcher / menu` | literal smoke phrase must remain or smoke must change in same commit |
| SMOKE-SKILL-002 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Natural mode aliases` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-003 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `pre-build approval stop` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-004 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `mandatory micro-grill` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-005 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Vault Pulse` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-006 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Project Map Bootstrap` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-007 | `GATE-CHECKABLE` | `SKILL.md` | production hook/code target + SKILL pointer, only after approval | existing/new pre-action gate required before movement | `improvements-status.sh` | machine-checkable command or gate contract; tests alone cannot be authoritative |
| SMOKE-SKILL-008 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `refresh --changed` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-009 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Agentic Readiness Layer` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-010 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Active Session Contract` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-011 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Background Handles` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-012 | `GATE-CHECKABLE` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `Current-State Pulse / Gate` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-013 | `CORE-ALWAYS` | `SKILL.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `--verify-feature <feature-or-path>` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-SKILL-014 | `SMOKE-CONTRACT` | `SKILL.md` | SKILL.md | smoke scripts | `Memory Router & Learning Loop` | literal smoke phrase must remain or smoke must change in same commit |
| SMOKE-SKILL-015 | `SMOKE-CONTRACT` | `SKILL.md` | SKILL.md | smoke scripts | `code-review ensemble` | literal smoke phrase must remain or smoke must change in same commit |
| SMOKE-REF-001 | `CORE-ALWAYS` | `reference.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `fixes the verified root cause` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-REF-002 | `CORE-ALWAYS` | `reference.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `non-contradictory` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-REF-003 | `CORE-ALWAYS` | `reference.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `no invented assumptions` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-REF-004 | `CORE-ALWAYS` | `reference.md` | CORE-ALWAYS unless approval packet proves safe target | always-loaded driver | `does it address the cause, not the symptom` | STOP/prohibition/global routing or no safe pre-action gate established in R2.1 |
| SMOKE-REF-005 | `SMOKE-CONTRACT+CORE-ALWAYS` | `reference.md` | reference.md | smoke scripts | ``kimiflow grill` — clarify/spec only, no code.` | literal smoke phrase must remain or smoke must change in same commit |
| SMOKE-REF-006 | `SMOKE-CONTRACT+CORE-ALWAYS+GATE-CHECKABLE` | `reference.md` | reference.md | smoke scripts | ``kimiflow plan` — clarify + understand + plan + plan-gate, then park/resume, no code.` | literal smoke phrase must remain or smoke must change in same commit |
| SMOKE-REF-007 | `SMOKE-CONTRACT+CORE-ALWAYS` | `reference.md` | reference.md | smoke scripts | ``kimiflow review` — read-only existing-feature/current-change review, no code.` | literal smoke phrase must remain or smoke must change in same commit |
| SMOKE-REF-008 | `SMOKE-CONTRACT+CORE-ALWAYS` | `reference.md` | reference.md | smoke scripts | ``kimiflow audit` — read-only cleanup/refactoring scan first, no code until a slice is approved.` | literal smoke phrase must remain or smoke must change in same commit |
| SMOKE-REF-009 | `SMOKE-CONTRACT` | `reference.md` | reference.md | smoke scripts | `CANDIDATE <SEVERITY> <ref> :: <claim> :: verify=<smallest check>` | literal smoke phrase must remain or smoke must change in same commit |

## SKILL.md Line Coverage

| Line | Coverage | IDs | Text / Justification |
|---:|---|---|---|
| 1 | `heading/context` | - | `---` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 2 | `frontmatter/context` | - | `name: kimiflow` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 3 | `protected-rule` | INV-001, SMOKE-SKILL-015 | `description: "Gated feature & bug-fix loop: clarify → research/diagnose (memory-first) → plan → plan-gate → implement → verify → red/gree...` — contains one or more corpus needles; no movement in R2.1 |
| 4 | `frontmatter/context` | - | `disable-model-invocation: false` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 5 | `protected-rule` | SMOKE-SKILL-013 | `argument-hint: [full\|grill\|plan\|build\|quick\|review\|audit\|fix] [<feature-or-bug>] [--launcher\|--menu] [--fix] [--audit <path>] [--...` — contains one or more corpus needles; no movement in R2.1 |
| 6 | `heading/context` | - | `---` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 8 | `heading/context` | - | `# kimiflow — Feature & Fix Loop` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 10 | `example/prose-only` | - | `Orchestrates the full loop for: **$ARGUMENTS**` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 12 | `example/prose-only` | - | `You are the **orchestrator**. Run the phases below as a state machine; delegate heavy work to subagents (fresh, isolated context); keep o...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 14 | `heading/context` | - | `## Modes (invocation)` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 16 | `protected-rule` | INV-002, INV-050, SMOKE-SKILL-001 | `- **Launcher / menu:** **`/kimiflow`**, **`/kimiflow --launcher`**, **`/kimiflow --menu`**, or a vague explicit Kimiflow request ("run Ki...` — contains one or more corpus needles; no movement in R2.1 |
| 17 | `example/prose-only` | - | `- **`/kimiflow <feature-or-bug>`** — full run (phases 0–7).` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 18 | `protected-rule` | INV-003, SMOKE-SKILL-002 | `- **Natural mode aliases:** **`/kimiflow full\|grill\|plan\|build\|quick\|review\|audit\|fix [target]`** and plain text such as **`kimifl...` — contains one or more corpus needles; no movement in R2.1 |
| 19 | `protected-rule` | INV-004 | `- **`full`** — strict full loop, scope=`large`: full grill/spec, understanding/research, plan + acceptance criteria, plan-gate, then **ST...` — contains one or more corpus needles; no movement in R2.1 |
| 20 | `protected-rule` | INV-005, INV-051 | `- **`grill`** — Phase 1 only: clarify/spec in plain language, write `INTENT.md`/`PROBLEM.md`, ask "Does this match?", then STOP. No plan ...` — contains one or more corpus needles; no movement in R2.1 |
| 21 | `protected-rule` | INV-006 | `- **`plan`** — prepare only: clarify + understand/diagnose + `PLAN.md`/`ACCEPTANCE.md` + plan-gate, then STOP with a resumable backlog ru...` — contains one or more corpus needles; no movement in R2.1 |
| 22 | `protected-rule` | INV-007 | `- **`build`** — implement an approved/prepared Kimiflow plan. If no current approved plan/backlog run is available, ask whether to run `f...` — contains one or more corpus needles; no movement in R2.1 |
| 23 | `protected-rule` | INV-008, INV-009, INV-095, INV-096, SMOKE-SKILL-004 | `- **`quick`** — lean run for small, low-risk work: mandatory micro-grill, normal verification, review light (= ONE code-review lens, `bug...` — contains one or more corpus needles; no movement in R2.1 |
| 24 | `protected-rule` | INV-010 | `- **`review`** — alias for `--verify-feature` / current-change review: read-only check of an already-built feature or current diff. No co...` — contains one or more corpus needles; no movement in R2.1 |
| 25 | `protected-rule` | INV-011 | `- **`audit`** — alias for `--audit <path>`: read-only cleanup/refactoring scan first; no edits until the user chooses a slice.` — contains one or more corpus needles; no movement in R2.1 |
| 26 | `example/prose-only` | - | `- **`fix`** — alias for `--fix`: bug flow with problem clarification, reproduction/Red evidence, root-cause proof, current fix research, ...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 27 | `protected-rule` | INV-012 | `- **`/kimiflow … --prepare`** — prepare only: phases 0–4, then STOP. Package in `.kimiflow/<slug>/`; implement later, even in a new session.` — contains one or more corpus needles; no movement in R2.1 |
| 28 | `protected-rule` | INV-013 | `- **`/kimiflow --resume <slug>`** — continue: read `.kimiflow/<slug>/STATE.md`, then run the resume safety check before implementation. A...` — contains one or more corpus needles; no movement in R2.1 |
| 29 | `example/prose-only` | - | `- **Feature or fix:** kimiflow detects whether you are building or fixing a bug, and routes accordingly. Force with **`/kimiflow --fix <b...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 30 | `protected-rule` | INV-014 | `- **Audit / cleanup mode:** kimiflow detects cleanup intent ("remove dead code", "over-engineering audit", "entschlacken", "clean up") an...` — contains one or more corpus needles; no movement in R2.1 |
| 31 | `protected-rule` | INV-015, SMOKE-SKILL-013 | `- **Existing feature check:** **`/kimiflow --verify-feature <feature-or-path>`** is review-only: independent lenses check an already-buil...` — contains one or more corpus needles; no movement in R2.1 |
| 32 | `example/prose-only` | - | `- **Explore mode (opt-in, divergent — feature only):** diverge on **direction** before locking the WHAT. Forced with **`/kimiflow --explo...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 33 | `protected-rule` | INV-016, INV-017, SMOKE-SKILL-006 | `- **Project Map Bootstrap (recommended, skippable):** **`/kimiflow --project-map <quick\|standard\|deep\|skip>`** controls the local proj...` — contains one or more corpus needles; no movement in R2.1 |
| 34 | `protected-rule` | INV-018 | `- **Display verbosity (visible output only — engine identical at every level):** `--quiet`/`--verbose` set the level for one run (never p...` — contains one or more corpus needles; no movement in R2.1 |
| 35 | `example/prose-only` | - | `- **Pre-build summary gate (project-local, default on):** end of Phase 4, before building: structured summary, **waits for your OK** — *a...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 37 | `heading/context` | - | `## Core principles (apply in ALL phases)` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 39 | `example/prose-only` | - | `- **Language: reply in the user's language.** Detect what the user writes in; use it for everything they see and the artifacts they revie...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 40 | `protected-rule` | INV-019 | `- **Terse output (HARD RULE — governs every phase; this is where runs bloat).** The `balanced` baseline (display-verbosity scales only th...` — contains one or more corpus needles; no movement in R2.1 |
| 41 | `example/prose-only` | - | `- **(a) One-line phase announcements** — marker + name + ≤1 clause. Never a paragraph.` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 42 | `protected-rule` | INV-020 | `- **(b) NEVER paste a full artifact into chat** (INTENT/PROBLEM/RESEARCH/DIAGNOSIS/PLAN/ACCEPTANCE). Write it to its file; show a ≤3-line...` — contains one or more corpus needles; no movement in R2.1 |
| 43 | `protected-rule` | INV-021 | `- **(c) Gate verdict = ONE line** — e.g. `gate open · open BLOCKER/HIGH: 0`. No narrative; reasoning lives in `REVIEW.md`.` — contains one or more corpus needles; no movement in R2.1 |
| 44 | `example/prose-only` | - | `- **(d) Evidence = the command + only the decisive output line(s)**, never a full log dump.` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 45 | `protected-rule` | INV-022 | `- **(e) No STATE *narration* in chat, no recap tables, no restating what a subagent will do or just did.** Use the Phase-0 task-list widg...` — contains one or more corpus needles; no movement in R2.1 |
| 46 | `example/prose-only` | - | `- **Budget: ≤~6 lines of your own prose per phase**, outside the required artifact-summary / decisive-evidence. Exempt: the Phase-7 commi...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 47 | `protected-rule` | INV-023 | `- **Artifact economy (terse output, for files).** On-disk artifacts (INTENT/PROBLEM/RESEARCH/DIAGNOSIS/PLAN/ACCEPTANCE/findings) are re-r...` — contains one or more corpus needles; no movement in R2.1 |
| 48 | `example/prose-only` | - | `- **Phase colors — announce each phase with its marker:** ⚪ 0 Setup · 🧭 Explore (opt-in) · 🔵 1 Clarify · 🟣 2 Understand · ⚫ 3 Plan · 🟡 4 ...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 49 | `protected-rule` | INV-024 | `- **Self-contained — the skill is the authority.** Every gate, threshold and standard lives here (+ reference.md), never in a personal/gl...` — contains one or more corpus needles; no movement in R2.1 |
| 50 | `protected-rule` | INV-025 | `- **Simplicity-first.** Minimal code/plan for the problem. No speculative abstractions, no features beyond the request.` — contains one or more corpus needles; no movement in R2.1 |
| 51 | `protected-rule` | INV-026 | `- **Anti-hallucination.** Only claims you can back. "Not verifiable" is valid. Severity never higher than provable by a code reference.` — contains one or more corpus needles; no movement in R2.1 |
| 52 | `protected-rule` | INV-027 | `- **Evidence-before-assertion.** Never claim "done/green/root cause found" without showing the actual command + output / the `file:line`.` — contains one or more corpus needles; no movement in R2.1 |
| 53 | `protected-rule` | INV-028 | `- **Agent budget.** Fan out to up to ~5–10 subagents automatically when it measurably improves the result. Beyond ~10 → stop and ask the ...` — contains one or more corpus needles; no movement in R2.1 |
| 54 | `example/prose-only` | - | `- **Model routing.** Allocate models by role: session model (strongest) → planner/implementer/verification; a **different model family** ...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 55 | `protected-rule` | INV-029 | `- **Persist phase progress (NOT optional, NOT terse-trimmable).** Phase 0 creates `.kimiflow/<slug>/STATE.md`; after finishing every phas...` — contains one or more corpus needles; no movement in R2.1 |
| 56 | `protected-rule` | INV-030, SMOKE-SKILL-010 | `- **Active Session Contract (not optional once Kimiflow starts).** A non-trivial explicit Kimiflow run starts `hooks/active-run.sh start ...` — contains one or more corpus needles; no movement in R2.1 |
| 57 | `protected-rule` | INV-031, INV-032, SMOKE-SKILL-011 | `- **Background Handles (optional, visible from launcher).** Long read-only or draft-producing work can be registered through `hooks/backg...` — contains one or more corpus needles; no movement in R2.1 |
| 58 | `protected-rule` | INV-033, SMOKE-SKILL-009 | `- **Agentic Readiness Layer (local, no network).** Before background-result trust, autonomous continuation, prepared-plan handoff reuse, ...` — contains one or more corpus needles; no movement in R2.1 |
| 59 | `protected-rule` | INV-034 | `- **Stop criteria always active:** success-stop (gate/verification met), failure-stop (escalate — see phase 5), budget-stop (cap reached ...` — contains one or more corpus needles; no movement in R2.1 |
| 60 | `protected-rule` | INV-035 | `- **Subagents do NOT see your context.** Every delegation carries: objective, output format, allowed files/boundaries, the paths of the r...` — contains one or more corpus needles; no movement in R2.1 |
| 62 | `heading/context` | - | `## ⚪ Phase 0 — Setup, Routing & Scope-Gate` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 64 | `protected-rule` | INV-036, INV-037 | `0. **Launcher routing (before slugging a normal run).** If the invocation is empty (`/kimiflow` / `$kimiflow` / `@kimiflow`), `--launcher...` — contains one or more corpus needles; no movement in R2.1 |
| 65 | `protected-rule` | INV-038, INV-039 | `0.5. **Working-tree start gate (normal write runs).** Before slugging any normal feature/fix/audit/project-map write run, run `${CLAUDE_P...` — contains one or more corpus needles; no movement in R2.1 |
| 66 | `example/prose-only` | - | `1. **Slug + state dir.** Derive a kebab-case `<slug>`. State lives under `.kimiflow/<slug>/` at the git root of the current project — in ...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 67 | `protected-rule` | INV-040, INV-041, SMOKE-SKILL-003, SMOKE-SKILL-013 | `2. **Mode routing — feature, fix, audit, feature-check, or natural alias.** First normalize a leading natural alias (`full\|grill\|plan\|...` — contains one or more corpus needles; no movement in R2.1 |
| 68 | `example/prose-only` | - | `3. **Resume check.** With `--resume <slug>`: read its `STATE.md`, resume at the first unfinished phase. Else if `.kimiflow/<slug>/STATE.m...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 69 | `protected-rule` | INV-030, INV-042 | `3.5. **Active session lock.** For every non-trivial normal run, immediately run `hooks/active-run.sh start --run .kimiflow/<slug> --mode ...` — contains one or more corpus needles; no movement in R2.1 |
| 70 | `example/prose-only` | - | `4. **Phase task-list (glance widget).** Create one task per phase you will actually run (`TaskCreate`), scaled to scope (trivial → the fe...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 71 | `protected-rule` | INV-043 | `5. **Git check.** `git rev-parse --is-inside-work-tree`. No repo → report + ask: `git init`, or run through verification only (phases 5–6...` — contains one or more corpus needles; no movement in R2.1 |
| 72 | `example/prose-only` | - | `6. **Scope-gate — hard rule (the default protects simplicity-first):**` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 73 | `example/prose-only` | - | `- **Default = `small`** — `large` multiplies subagent/round (token) cost, so it's the exception, not a reflex. Bump to `large` only if ≥~...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 74 | `example/prose-only` | - | `- **`trivial`** = 1–2 files, no risk, and the user's requested output is exact enough that no user-visible behavior can be misread (fix: ...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 75 | `protected-rule` | INV-044, SMOKE-SKILL-003, SMOKE-SKILL-004 | `- In doubt, the smaller tier. Effect: trivial → no loop, no grill (implement/fix, verify briefly, commit-gate). small → reduced loop with...` — contains one or more corpus needles; no movement in R2.1 |
| 76 | `protected-rule` | INV-045 | `7. **Display verbosity + first-run onboarding** (resolve at the very start — governs print volume, never the engine; full rules → referen...` — contains one or more corpus needles; no movement in R2.1 |
| 77 | `example/prose-only` | - | `- **(a)** Map any `--quiet`/`--verbose` to a level; `RV get [--flag <level>]` → the level that governs your output.` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 78 | `protected-rule` | INV-046 | `- **(b)** `RV onboard-check [--flag <level>]` → `ASK`\|`SKIP` (mechanical). `ASK` ∧ interactive → you MUST ask once for level + save-scop...` — contains one or more corpus needles; no movement in R2.1 |
| 79 | `example/prose-only` | - | `- **(c)** Utility flags: `--set-verbosity <level>` → `RV set project`; `--settings` (dialog) → `RV set <scope>`. Write, report path, exit.` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 80 | `protected-rule` | INV-047, INV-048, SMOKE-SKILL-006 | `8. **Project Map Bootstrap / status check** (recommended, never a prerequisite; full rules → reference.md "Project Map Bootstrap"). `PMS`...` — contains one or more corpus needles; no movement in R2.1 |
| 82 | `heading/context` | - | `## 🧭 Explore (opt-in, before Phase 1) — diverge on direction` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 84 | `example/prose-only` | - | `Runs only on `--explore` or an accepted offer (→ reference.md "Explore phase"). **Feature only** — a fix/cleanup that surfaces → suggest ...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 86 | `protected-rule` | INV-049 | `1. **Bound (≤1 question)** — one plain-language question only if the request lacks the goal/constraints to explore relevantly; else skip.` — contains one or more corpus needles; no movement in R2.1 |
| 87 | `example/prose-only` | - | `2. **Fan-out** — 2–3 read-only `Explore` agents, each forced to a DISTINCT direction, codebase-grounded (`file:line`; "NOT VERIFIED" if s...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 88 | `example/prose-only` | - | `3. **Menu** — synthesize 2–3 distinct directions (each ≤3 lines), write `EXPLORE.md`, show menu + path (bounded terse-output exemption).` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 89 | `protected-rule` | INV-050 | `4. **Pick (gate, human):** **continue** → chosen direction seeds Phase 1 `INTENT.md` → normal loop. **stop** → like `--prepare` (STOP, up...` — contains one or more corpus needles; no movement in R2.1 |
| 91 | `heading/context` | - | `## 🔵 Phase 1 — Clarify (plain language): Intent (feature) or Problem (fix)` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 93 | `protected-rule` | SMOKE-SKILL-004 | `Goal: shared understanding BEFORE research/plan. kimiflow clarifies itself (embedded), always in plain language (everyday words, one ques...` — contains one or more corpus needles; no movement in R2.1 |
| 95 | `protected-rule` | INV-051 | `- **Feature → intent clarification:** clarify goal, value, in/out of scope, "what done looks like" → write `INTENT.md` → **gate** "Does t...` — contains one or more corpus needles; no movement in R2.1 |
| 96 | `protected-rule` | INV-052 | `- **Fix → problem clarification:** symptom, expected vs. actual, when/how it occurs (steps, logs, since when, always/intermittent) → writ...` — contains one or more corpus needles; no movement in R2.1 |
| 97 | `protected-rule` | INV-053 | `- **Audit → scope clarification:** which paths, how aggressive, behavior-preserve constraints, do-NOT-touch hints, "what stays untouched"...` — contains one or more corpus needles; no movement in R2.1 |
| 98 | `protected-rule` | INV-054, INV-055 | `- **Mechanical clarify gate:** before Phase 2, run `${CLAUDE_PLUGIN_ROOT:-$CLAUDE_SKILL_DIR}/hooks/clarify-gate.sh .kimiflow/<slug>` (Cod...` — contains one or more corpus needles; no movement in R2.1 |
| 100 | `heading/context` | - | `## 🟣 Phase 2 — Understand & research / diagnose (memory-first → recall → understanding ∥ web → synthesis → save)` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 102 | `example/prose-only` | - | `Goal: kimiflow must truly understand the affected code before planning — evidence-based. Full checklists: → reference.md "Understand & re...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 104 | `protected-rule` | INV-056, SMOKE-SKILL-014 | `0. **Project memory first** (cheap, all tiers). Read the project's `AGENTS.md`/`CLAUDE.md` conventions plus `.kimiflow/STANDARDS.md` and ...` — contains one or more corpus needles; no movement in R2.1 |
| 105 | `protected-rule` | INV-057, INV-058, SMOKE-SKILL-005 | `1. **Recall before researching** (local first, providers optional). Build key terms from `INTENT.md`/`PROBLEM.md`/`AUDIT-INTENT.md`, run ...` — contains one or more corpus needles; no movement in R2.1 |
| 106 | `protected-rule` | INV-059, INV-060, SMOKE-SKILL-012 | `1a. **Current-State Pulse / Gate** (source freshness before Spec/Plan, token-cheap even for `small`). `CSG` = `${CLAUDE_PLUGIN_ROOT:-$CLA...` — contains one or more corpus needles; no movement in R2.1 |
| 108 | `protected-rule` | INV-048, INV-061, SMOKE-SKILL-006 | `1b. **Section lookup** (map-backed runs, token-cheap suggestion — never a gate). When `.kimiflow/project/INDEX.json` exists, run `${CLAUD...` — contains one or more corpus needles; no movement in R2.1 |
| 110 | `example/prose-only` | - | `**Feature → understand & research:**` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 111 | `example/prose-only` | - | `2. **Codebase understanding** (read-only, `Explore` agent, input `INTENT.md` + project memory) → the checklist in reference.md "Understan...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 112 | `example/prose-only` | - | `3. **External research** (`general-purpose` + `WebSearch`/context7/`WebFetch`): for `small`/`quick`, always do the Current-State Pulse fi...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 113 | `protected-rule` | INV-062 | `4. **Synthesis → `RESEARCH.md`** (structure in reference.md, incl. open unknowns). **Mini-gate:** a plan-blocking unknown → resolve first...` — contains one or more corpus needles; no movement in R2.1 |
| 115 | `example/prose-only` | - | `**Fix → understand & diagnose** (prove first, then fix):` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 116 | `protected-rule` | INV-063 | `2. **Reproduce** — actually trigger the bug, ideally a failing test (proof: real + where). Write the Red evidence to `BUG-REPRO.md` befor...` — contains one or more corpus needles; no movement in R2.1 |
| 117 | `protected-rule` | INV-064 | `3. **Verify the root cause** (input `PROBLEM.md`) — find AND prove the cause (`file:line` + why that spot produces the symptom). NOT the ...` — contains one or more corpus needles; no movement in R2.1 |
| 118 | `example/prose-only` | - | `4. **Fix research (proactive, BEFORE the fix)** — how is this *currently* solved correctly? Recall (vault/claude-mem) → `WebSearch`/conte...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 119 | `protected-rule` | INV-065 | `5. **Synthesis → `DIAGNOSIS.md`** (→ reference.md "Fix mode"). **Diagnosis gate:** root cause not proven → do NOT fix (keep investigating...` — contains one or more corpus needles; no movement in R2.1 |
| 121 | `example/prose-only` | - | `**Audit → find the fat** (read-only, evidence-based):` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 122 | `protected-rule` | INV-066 | `2. **Survey the target** (`Explore` agent, input `AUDIT-INTENT.md`): map what exists and why. For each non-trivial item ask the **existen...` — contains one or more corpus needles; no movement in R2.1 |
| 123 | `protected-rule` | INV-067, INV-068 | `3. **Tag findings** `yagni`/`delete`/`shrink`/`stdlib` — each with `path:line` + replacement + a repo-wide pre-delete grep (→ 0 for `dele...` — contains one or more corpus needles; no movement in R2.1 |
| 124 | `protected-rule` | INV-069 | `4. **Synthesis → `AUDIT.md`**: self-contained **slices** ranked biggest-cut-first + a **do-NOT-touch** list. **Caller-grep is a MINIMUM**...` — contains one or more corpus needles; no movement in R2.1 |
| 126 | `protected-rule` | INV-070 | `**Always last — vault-save** (automatic — only if a vault MCP is connected; else skip + note in STATE) per → reference.md "Vault conventi...` — contains one or more corpus needles; no movement in R2.1 |
| 128 | `heading/context` | - | `## ⚫ Phase 3 — Plan (testable acceptance criteria)` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 130 | `example/prose-only` | - | `Delegate to a `general-purpose` planner (or the read-only `Plan` agent + you persist). Inputs: `INTENT.md`+`RESEARCH.md` (or `PROBLEM.md`...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 132 | `protected-rule` | INV-071 | `**(large) Dual-plan, selection-first:** TWO independent planners in parallel with distinct framings (minimal-first vs risk-first), one cr...` — contains one or more corpus needles; no movement in R2.1 |
| 134 | `example/prose-only` | - | `- `PLAN.md`: minimal, aligned with the existing architecture (and project standards); task breakdown; mark each task independent (file-di...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 135 | `protected-rule` | INV-072 | `- `PLAN.md` must be deterministic enough that two implementers can produce the same behavioral result: declare affected files/paths (or e...` — contains one or more corpus needles; no movement in R2.1 |
| 136 | `example/prose-only` | - | `- `ACCEPTANCE.md`: each criterion per template (EARS + concrete input→output + named verification method) with an explicit `AC-N → test_n...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 137 | `example/prose-only` | - | `- **(large) Considered alternatives:** produced by the dual-plan selection — record the losing approach + the selecting trade-off in `PLA...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 139 | `heading/context` | - | `## 🟡 Phase 4 — Plan-gate (loop, binary, cap 3)` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 141 | `example/prose-only` | - | `Read → reference.md "Review rubric".` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 143 | `protected-rule` | INV-073, INV-074 | `0. **Mechanical plan-blocker gate (before round 1):** run `${CLAUDE_PLUGIN_ROOT:-$CLAUDE_SKILL_DIR}/hooks/plan-blocker-gate.sh .kimiflow/...` — contains one or more corpus needles; no movement in R2.1 |
| 144 | `example/prose-only` | - | `1. Spawn **2 independent reviewers in parallel** (scope=small → 1, lens B), fresh context, seeing only `PLAN.md` + `ACCEPTANCE.md` + `INT...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 145 | `example/prose-only` | - | `- **A — goal/completeness & understanding** (goal-backward; canonical definition → reference.md "Review rubric").` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 146 | `example/prose-only` | - | `- **B — risk:** security/edge/error/architecture/over-engineering (canonical definition incl. the fix-mode cause-not-symptom check → rubr...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 147 | `protected-rule` | INV-075, INV-076 | `- **(audit) refute the cut:** for each `delete`/`yagni` slice, actively hunt a **live caller** (repo-wide, incl. dynamic dispatch / refle...` — contains one or more corpus needles; no movement in R2.1 |
| 148 | `protected-rule` | INV-077 | `- Each reviewer gives reasoning before verdict, then writes this round's findings to `.kimiflow/<slug>/findings/r<N>-<lens>.md` in the ca...` — contains one or more corpus needles; no movement in R2.1 |
| 149 | `example/prose-only` | - | `2. Append a human-readable round summary to `REVIEW.md` (narrative only — reasoning, not the gate truth).` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 150 | `protected-rule` | INV-078, INV-079 | `3. **Gate (binary, NO numeric score):** count open BLOCKER/HIGH via `${CLAUDE_PLUGIN_ROOT:-$CLAUDE_SKILL_DIR}/hooks/resolve-review-gate.s...` — contains one or more corpus needles; no movement in R2.1 |
| 151 | `protected-rule` | INV-080 | `4. **Anti-oscillation (blocker-aware):** open BLOCKER/HIGH count doesn't strictly decrease across a round, or a disappeared finding reapp...` — contains one or more corpus needles; no movement in R2.1 |
| 152 | `protected-rule` | INV-080, INV-081 | `5. **Cap (3) reached without an open gate → stop + ask, gate CLOSED (never auto-proceed).**` — contains one or more corpus needles; no movement in R2.1 |
| 153 | `protected-rule` | INV-082 | `6. **Gate open →** `--prepare`: STOP, update STATE (0–4 done, `Status: backlog`), output `/kimiflow --resume <slug>`. Else → step 7.` — contains one or more corpus needles; no movement in R2.1 |
| 154 | `protected-rule` | INV-082, INV-083, INV-084, INV-085 | `7. **Pre-build summary gate** (project-local, default on; control-flow only, never changes the engine — toggle via `--settings`; → refere...` — contains one or more corpus needles; no movement in R2.1 |
| 156 | `heading/context` | - | `## 🟠 Phase 5 — Implement / fix` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 158 | `example/prose-only` | - | `**Default: 1 implementation subagent, sequential.** Full tools, fresh context, inputs `PLAN.md` + `ACCEPTANCE.md` + `INTENT.md`/`PROBLEM....` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 160 | `protected-rule` | INV-086 | `- **TDD where sensible:** failing test first (Red) → commit tests before the implementation → green → refactor. The Red test commit is th...` — contains one or more corpus needles; no movement in R2.1 |
| 161 | `protected-rule` | INV-087 | `- **Surgical:** every changed line traces to plan/intent/diagnosis. Leave foreign code alone, clean your own orphans. Every deletion carr...` — contains one or more corpus needles; no movement in R2.1 |
| 162 | `protected-rule` | INV-088 | `- **Escalation keyed on the FIRST failure's signal** (failure-stop): a clear test/stack failure → one targeted execution-feedback fix; an...` — contains one or more corpus needles; no movement in R2.1 |
| 164 | `heading/context` | - | `## 🟤 Phase 6 — Verify against acceptance criteria (goal-backward)` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 166 | `example/prose-only` | - | `Run each check, show real output, prove the goal — full checklist (incl. the ✓/⚠/✗ marking scheme, cold-start trigger list, LSP tool sele...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 168 | `example/prose-only` | - | `- **Run each criterion's method** and show the command + the decisive result line(s) — not full logs.` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 169 | `example/prose-only` | - | `- **Goal-backward:** for each criterion's artifact check Exists / Substantive / Wired — "task done ≠ goal achieved".` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 170 | `protected-rule` | INV-089, INV-090 | `- **Fix mode (mandatory):** the reproduction no longer fails. Then run `${CLAUDE_PLUGIN_ROOT:-$CLAUDE_SKILL_DIR}/hooks/red-green-gate.sh ...` — contains one or more corpus needles; no movement in R2.1 |
| 171 | `protected-rule` | INV-091 | `- **Local diagnostics advisory:** when code changed, run `${CLAUDE_PLUGIN_ROOT:-$CLAUDE_SKILL_DIR}/hooks/lsp-diagnostics.sh` and append a...` — contains one or more corpus needles; no movement in R2.1 |
| 172 | `example/prose-only` | - | `- **Regression:** existing/affected test suite green.` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 173 | `example/prose-only` | - | `- Cold-start smoke test when the diff touches boot-critical files; non-automatable criteria → verifier subagent (derives pass/fail from e...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 174 | `protected-rule` | INV-092 | `- **(large) Independent verifier — additive:** one implementer-blind verifier (cross-family read-only when available) re-derives the swee...` — contains one or more corpus needles; no movement in R2.1 |
| 175 | `protected-rule` | INV-093 | `- Any failure → back to phase 5 (escalation rule applies).` — contains one or more corpus needles; no movement in R2.1 |
| 177 | `heading/context` | - | `## 🟢 Phase 7 — Code-review ensemble against specs → fix → commit-gate` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 179 | `protected-rule` | INV-094 | `1. **Review packet.** Build one compact packet for reviewers: changed-file list, `git diff --stat`, relevant diff hunks, `ACCEPTANCE.md`,...` — contains one or more corpus needles; no movement in R2.1 |
| 180 | `protected-rule` | INV-095, INV-096, INV-097 | `2. **Candidate lenses in parallel.** Read → reference.md "Review rubric" (orchestrator only). Spawn narrow fresh-context reviewers with a...` — contains one or more corpus needles; no movement in R2.1 |
| 181 | `protected-rule` | INV-096 | `- **R1 bug-regression:** logic, edge cases, acceptance drift, weakened/insufficient tests.` — contains one or more corpus needles; no movement in R2.1 |
| 182 | `example/prose-only` | - | `- **R2 failure-security:** input validation, secrets/privacy, path handling, rollback/failure atomicity, partial writes.` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 183 | `example/prose-only` | - | `- **R3 integration-contract:** installed hooks/metadata/docs/launcher wiring, host parity (Claude/Codex), command/API/schema contracts.` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 184 | `protected-rule` | INV-098 | `Each lens writes `.kimiflow/<slug>/code-review-candidates/r<N>-<lens>.md` using `CANDIDATE <SEVERITY> <ref> :: <claim> :: verify=<smalles...` — contains one or more corpus needles; no movement in R2.1 |
| 185 | `protected-rule` | INV-099, INV-100 | `3. **Orchestrator verification.** Verify each candidate with targeted code reads, commands, or reproduction; for BLOCKER/HIGH candidates ...` — contains one or more corpus needles; no movement in R2.1 |
| 186 | `protected-rule` | INV-091, INV-101, INV-102, INV-103 | `4. **Advisories.** First stage the run's named changed paths (`git add <named paths>` — never `-A`; the scans read the staged diff). Then...` — contains one or more corpus needles; no movement in R2.1 |
| 187 | `protected-rule` | INV-078, INV-104 | `5. **Fix.** Fix confirmed BLOCKER/HIGH, then re-run the candidate lenses affected by the fix and repeat verification. Resolve with `${CLA...` — contains one or more corpus needles; no movement in R2.1 |
| 188 | `protected-rule` | INV-105, INV-106, INV-107, INV-108 | `6. **Commit-gate — STOP.** Read → reference.md "Commit hygiene". **Advisory triage (fail-closed):** present every `.kimiflow/<slug>/ADVIS...` — contains one or more corpus needles; no movement in R2.1 |
| 189 | `protected-rule` | INV-109, INV-110, INV-111, INV-112, INV-113, INV-114, SMOKE-SKILL-014 | `7. **Learning loop + project memory — required before `Status: done`.** Run `MR review-run --run .kimiflow/<slug> --write` after verifica...` — contains one or more corpus needles; no movement in R2.1 |
| 190 | `protected-rule` | INV-047, INV-115, INV-116, SMOKE-SKILL-006, SMOKE-SKILL-008 | `8. **Project-map auto-refresh** (recommended, non-blocking). After Phase-6 verify, run `PMS refresh --changed` (`PMS` = `${CLAUDE_PLUGIN_...` — contains one or more corpus needles; no movement in R2.1 |
| 191 | `protected-rule` | INV-117, INV-118, INV-119, SMOKE-SKILL-007 | `8a. **Workqueue close-back** (recommended, non-blocking). If this run implemented a slice from the local workqueue (`.kimiflow/project/IM...` — contains one or more corpus needles; no movement in R2.1 |
| 192 | `protected-rule` | INV-120 | `9. **Close state.** Only after the commit gate and learning review are open, set all phases done and set `Status: done` in `STATE.md`.` — contains one or more corpus needles; no movement in R2.1 |
| 194 | `heading/context` | - | `## Scaling knobs (defaults scale with scope — the cross-family lens is ON when available; extras stay OFF until enabled within the agent ...` — structure/frontmatter needed to orient the driver; no independent rule movement proposed in R2.1 |
| 196 | `protected-rule` | INV-121 | `> **Display verbosity is NOT a knob.** Always-on, changes only visible output volume — never gates, cost, quality, or behavior. Never cou...` — contains one or more corpus needles; no movement in R2.1 |
| 198 | `example/prose-only` | - | `- **Parallel implementation (incl. merge):** ≥2 genuinely independent, file-disjoint, small tasks → implementers with `isolation: worktre...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 199 | `protected-rule` | INV-122, INV-123 | `- **Best-of-N with tests:** a hard, fully test-encoded task → 2–3 candidate implementations in parallel worktrees, keep the one passing t...` — contains one or more corpus needles; no movement in R2.1 |
| 200 | `example/prose-only` | - | `- **Cross-family reviewer — now the default, not a knob:** one plan-gate lens and one code-review lens route to a different model family ...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 201 | `example/prose-only` | - | `- **Dedicated Simplicity prosecutor:** a blind, adversarial reviewer whose ONLY job is the Simplicity dimension (→ reference.md "Review r...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 202 | `example/prose-only` | - | `- **Multi-run gate:** for `large`/critical, take the reviewer's binary verdict 3× by majority (variance reduction).` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 203 | `example/prose-only` | - | `- **Deeper debugging:** for a stubborn bug, stop patching and run a systematic, hypothesis-first pass (reproduce → isolate → root-cause) ...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 204 | `example/prose-only` | - | `- **Hard test-gate (opt-in, per project):** kimiflow ships a Stop hook (`hooks/`) that blocks finishing on red tests — see → reference.md...` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 205 | `example/prose-only` | - | `- **Anti-reward-hacking hardening (critical code):** held-out/hidden tests, stricter diff inspection for test manipulation.` — context/detail line; operative protected clauses are tracked by corpus rows when present, otherwise no movement proposed in R2.1 |
| 206 | `protected-rule` | INV-124 | `- **Behavioral evals (out-of-CI, on-demand):** pressure-test the gates against rationalization with subagents loaded with the real skill ...` — contains one or more corpus needles; no movement in R2.1 |
